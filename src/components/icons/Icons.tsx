import * as React from "react";
import { LucideIcon, icons } from "lucide-react";

// Exportamos todos los iconos de lucide-react en un objeto
export const Icons: Record<string, LucideIcon> = { ...icons };
